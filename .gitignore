# Others
.*
*.md
*.jsonl
*.json
*.txt
CES/
StatCan/

# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# testing
/coverage
/test-results
/playwright-report
/playwright/.cache

# env
.env
.env.local
.env*.local
env.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode
tsconfig.tsbuildinfo
next-env.d.ts

# Backboard local manifests (contain assistant IDs; never commit)
.backboard/

# Roster reconciliation reports may include remote assistant IDs
docs/backboard/roster-reconciliation/

# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Data: raw citywide downloads are bounded-slice inputs, not outputs.
# Only data/processed/ (the clipped, reprojected twin inputs) is tracked.
data/raw/
OpinionQA/
ANES/

# Local model serving and HF cache (large, host-specific)
.vllm-env/
.hf_cache/

# Regenerable Phase 1 eval outputs (keep committed gate evidence snapshots)
eval/output/*
!eval/output/phase1_gate_evidence/
!eval/output/phase2_gate_evidence/
